Tree: 
    App.jsx
        Message.jsx

Virtual DOM
    package.json
        dependencies
            react
            react-dom

    index.html
        "root": container of application
        references: main.jsx 

    main.jsx
        component tree:
            ReactDOM(library) renders:
                React.StrictMode
                    App
        
            ReactNative -> mobile devices

